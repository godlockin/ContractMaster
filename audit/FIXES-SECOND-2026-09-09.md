# 第二轮修复记录

- B1：失败记录合并历史元数据，保留原件与脱敏摘要；连续失败后再次重试仍传入原摘要。
- B2：下载 main 返回状态码，CLI 传播该状态；任一失败返回 2，全部成功返回 0。
- B3：原件与实体词典只读取一次字节快照供解析、摘要使用。复核发布时继续对当前源文件核验，版本变化拒绝发布。
- B4：普通与深度校验共享 URL/日期检查。完整状态要求 finding 的 verified 依据通过 source_id 绑定法源目录，核对标题、URL、日期与条文。
- B5：公共包先写入私有暂存目录，审计就绪成功后原子移动；提交凭据 release.json 随包移动。审计写入失败不创建 public，恢复后重试成功。
- O2：每行 token 单次遍历，切片使用原字符串索引，避免反复扫描/复制剩余后缀。

验证：技能 33 项、人工复核包 6 项、演示及回归 10 项；共 49 项，48 通过，1 因缺少可选 pypdf 跳过。另执行 6 个合成案例流程验收。故障注入覆盖连续下载失败、源文件中途更新、无效/未关联法源及审计失败重试；分段检查 token 边界、偏移与全文往返。

40,000 个对齐 token 单元的本机单轮样本：约 0.3524 秒降至 0.0051 秒，约 69 倍加速。属于合成边界样本，不代表所有文档。

兼容变化：历史 verified 依据未关联 source_id 时仍可保留初稿，但不能申报完整深度；审计 RELEASE_COMMIT_READY 只表示就绪，以公共提交凭据确认提交。未执行联网语料下载或真实模型法律质量测试。修改未提交 Git。
